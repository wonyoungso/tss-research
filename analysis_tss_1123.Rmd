---
title: "analysis_tss_experiment_1123"
author: "Wonyoung So"
date: "11/23/2021"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
require(foreign)
require(tidyverse)
require(rdrobust)
require(stargazer)
require(httr)
require(jsonlite)
require(ggplot2)
require(dplyr)
require(miceadds)
library(miceadds)
require(texreg)
library(sandwich)
library(lmtest)
library(nnet)
```

## Import Data

```{r importdata, include=FALSE, message = FALSE}
tss_df
tss_df <- fromJSON("./mturk_landlords_decisions_06-2021.json")
tss_df$accept_type <- as.factor(tss_df$accept_type)
tss_df$accept_type <- relevel(tss_df$accept_type, ref="Yes")

tss_df$view_mode <- as.factor(tss_df$view_mode)
tss_df$view_mode <- relevel(tss_df$view_mode, ref="Type 1")

tss_df$score <- as.factor(tss_df$score)
tss_df$score <- relevel(tss_df$score, ref="LOW - CLEAN")

mymodel <- multinom(formula = accept_type ~ score, data=tss_df)
mymodel %>% summary()
```